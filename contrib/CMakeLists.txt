
if (LIBUILD)
	add_subdirectory(libuild)
	add_subdirectory(binreloc)
endif (LIBUILD)
if (PATCHELF)
	add_subdirectory(patchelf)
endif (PATCHELF)
if (VISUAL_LDD)
	add_subdirectory(visual-ldd)
endif (VISUAL_LDD)
